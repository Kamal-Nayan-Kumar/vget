--extra-index-url https://download.pytorch.org/whl/cpu

# Core — always required
# (no external deps needed for the base scanner)

# AI / CodeBERT — optional but recommended
# Install these for full AI analysis:
transformers>=4.35.0
torch>=2.0.0

# If you want to train your own classifier on CodeBERT embeddings:
scikit-learn>=1.3.0
numpy>=1.24.0